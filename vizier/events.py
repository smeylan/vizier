def eventHandler(args):
	# call processEvent   

	raise NotImplementedError	

	# for cases where we are simply running a single event, outside of an update context (e.g. a timed API call)
    



def processEvent(args):
	# if event_type == 'email':
	# "email_id" is the copy for the emails
	# "vars" is a list of strings to replace with the value in the email Template
	# elif event_type == 'api':            
	# "url" is the URL of the API endpoint to hit
	# "json_var": the key is used as the key, and the value is used to retrieve a value from user_vars
	# "json_string": key and value are both added verbatim to the POST JSON body
	raise NotImplementedError	